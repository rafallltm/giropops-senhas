apiVersion: v1
kind: Service
metadata:
  name: redis-service
  labels:
    app: redis
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: redis
  ports:
    - protocol: TCP
      port: {{ .Values.redis.service.port }}
      targetPort: 6379
  type: {{ .Values.redis.service.type }}
